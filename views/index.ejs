<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=, initial-scale=1.0" />

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous" />

  <link href="/styles/index.css" rel="stylesheet" />
  <title>Todolist</title>
</head>

<body>
  <div class="container">
    <!-- <%- include('header.ejs'); %> -->
    <h1>Todo List</h1>

    <p>
      <%= date %>
    </p>

    <div>
      <form action="/" method="post">
        <input type="text" name="item" />
        <button type="submit" name="button" class="btnn">+</button>
      </form>
    </div>

    <div>
      <% for (let i=0; i < items.length; i++) { %>

        <form action="/delete" method="post">
          <div class="item">
            <input type="checkbox" name="checked" value="<%= items[i]._id %>" />
            <p id="todo">
              <%= items[i].name %>
            </p>


            <button type="submit" name="delbut" value="<%= items[i]._id %>" class="btn btn-danger">×</button>
          </div>
        </form>

        <% } %>
    </div>
  </div>

  <%- include('footer.ejs'); %>

    <!-- <script>
      var d = document.querySelector("p");
      d.innerHTML = "asas";
      console.log(d);
            </script> -->
</body>

</html>